import { KeytrackDirective } from './keytrack.directive';

describe('KeytrackDirective', () => {
  it('should create an instance', () => {
    const directive = new KeytrackDirective();
    expect(directive).toBeTruthy();
  });
});
